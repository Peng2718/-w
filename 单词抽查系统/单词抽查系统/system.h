#pragma once
#ifndef SYSTEM_H
#define SYSTEM_H
#include "Word.h"
using namespace std;
class System {
public:
	void handle(word& Word, int& is_goon);
	void handle2(word& Word, int& is_goon);
};
#endif // !SYSTEM_H
